// ---------------------------------------------------------------------------
// Ingestion barrel — exposes only the public API
// ---------------------------------------------------------------------------

export { ingestFile } from './parsers';
export type { ParseResult, SupportedFormat } from './types';
